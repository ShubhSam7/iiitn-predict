{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "go build -o bin/server main.go",
    "dev": "dev.cmd",
    "start": "./bin/server",
    "lint": "golangci-lint run"
  }
}
